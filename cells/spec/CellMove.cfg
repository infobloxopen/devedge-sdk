\* TLC model configuration for CellMove.tla.
\* Small bounds keep the state space exhaustive yet fast.
CONSTANTS
    Cells = {a, b}
    MaxEpoch = 4
    MaxEventSeq = 3
INIT Init
NEXT Next
INVARIANT Safety
