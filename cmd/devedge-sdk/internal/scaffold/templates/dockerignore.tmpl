# Keep the Docker build context small. gen/ is intentionally NOT ignored: it holds
# the generated code the image build compiles (produced by `make generate` before
# the build) — excluding it would break the build.
.git
.github
bin/
dist/
deploy/
*.md
.env
.env.*
