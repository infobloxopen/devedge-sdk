---
title: Health & Readiness Probes
weight: 2
aliases:
  - /docs/guides/health/
---

A service built with the SDK exposes health and readiness probes out of the box — no extra wiring
required. The scaffold registers a DB-ping readiness check for you; adding domain-specific checks
is a one-liner.

## What is served

| Surface | Endpoint | Behaviour |
|---------|----------|-----------|
| HTTP (gateway) | `GET /healthz` | **Liveness.** `200` as long as the process serves. No dependency checks — a dep blip never kills a live pod. |
| HTTP (gateway) | `GET /readyz` | **Readiness.** `200` when all registered readiness checks pass; `503` + JSON listing failures when any fails. |
| gRPC | `grpc.health.v1.Health/Check` | Reflects the same readiness state: `SERVING` when ready, `NOT_SERVING` when any check fails. |

Both HTTP endpoints are **unauthenticated** (not subject to the authz interceptor) so the kubelet
can reach them without credentials. The gRPC health service methods are declared `Public` in the
authz rule set automatically by `server.New`.

When `HTTPAddr` is empty (gRPC-only mode), only the gRPC health service is exposed.

## Readiness-check seam

The readiness-check interface lives in `github.com/infobloxopen/devedge-sdk/health`:

```go
type Check interface {
    Name() string                    // stable key; appears in /readyz JSON on failure
    Check(ctx context.Context) error // nil = ready; non-nil = not ready
}
```

`server.Config` gains a `ReadinessChecks []health.Check` field. An empty slice means "always ready"
(the liveness default). Each check is bounded by a **2-second per-check timeout** so a hung
dependency cannot stall the probe indefinitely.

## Built-in DB-ping check

The only built-in check pings a `*database/sql.DB` (or anything with a `PingContext` method):

```go
import (
    sdkhealth "github.com/infobloxopen/devedge-sdk/health"
    "github.com/infobloxopen/devedge-sdk/server"
)

sqlDB, _ := gormDB.DB()   // or sql.Open(...)
s, err := server.New(server.Config{
    // ...
    ReadinessChecks: []sdkhealth.Check{
        sdkhealth.NewDBCheck("db", sqlDB),
    },
})
```

`NewDBCheck` accepts any `Pinger` (interface `{ PingContext(context.Context) error }`), satisfied by
`*sql.DB` from both GORM (`db.DB()`) and ent (`sql.Open`). No ORM type enters `server.Config`.

The scaffolded `main.go` / `main.ent.go` registers this check by default.

## Custom readiness checks

Implement `health.Check` for any dependency:

```go
type cacheCheck struct{ c *redis.Client }

func (c *cacheCheck) Name() string { return "cache" }
func (c *cacheCheck) Check(ctx context.Context) error {
    return c.c.Ping(ctx).Err()
}
```

Then register it alongside the DB check:

```go
ReadinessChecks: []sdkhealth.Check{
    sdkhealth.NewDBCheck("db", sqlDB),
    &cacheCheck{c: redisClient},
},
```

A failure body from `/readyz` looks like:

```json
{"status":"unready","checks":{"cache":"dial tcp ...: connection refused"}}
```

## Kubernetes wiring

Point your k8s probe spec at the HTTP endpoints:

```yaml
livenessProbe:
  httpGet:
    path: /healthz
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /readyz
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 10
```

Or use the gRPC health check (requires k8s ≥ 1.24 with gRPC probe support):

```yaml
livenessProbe:
  grpc:
    port: 9090
readinessProbe:
  grpc:
    port: 9090
```

The Helm chart generated by F035 (#95) wires these automatically.

## Dependency notes

`grpc/health` and `grpc/health/grpc_health_v1` are sub-packages of
`google.golang.org/grpc`, which is already in the module graph. No new transitive
dependency is added. The `health` package itself depends only on `context` and `time` from
the standard library — no ORM, no SDK.
