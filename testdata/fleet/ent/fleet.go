// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/infobloxopen/devedge-sdk/testdata/fleet/ent/fleet"
)

// Fleet is the model entity for the Fleet schema.
type Fleet struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Tenant discriminator — all queries are automatically scoped to this value.
	AccountID string `json:"account_id,omitempty"`
	// AIP-148 soft-delete timestamp; nil for live entities.
	DeleteTime *time.Time `json:"delete_time,omitempty"`
	// DisplayName holds the value of the "display_name" field.
	DisplayName string `json:"display_name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FleetQuery when eager-loading is set.
	Edges        FleetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FleetEdges holds the relations/edges for other nodes in the graph.
type FleetEdges struct {
	// Vehicles holds the value of the vehicles edge.
	Vehicles []*Vehicle `json:"vehicles,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// VehiclesOrErr returns the Vehicles value or an error if the edge
// was not loaded in eager-loading.
func (e FleetEdges) VehiclesOrErr() ([]*Vehicle, error) {
	if e.loadedTypes[0] {
		return e.Vehicles, nil
	}
	return nil, &NotLoadedError{edge: "vehicles"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Fleet) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case fleet.FieldID, fleet.FieldAccountID, fleet.FieldDisplayName:
			values[i] = new(sql.NullString)
		case fleet.FieldDeleteTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Fleet fields.
func (_m *Fleet) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case fleet.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case fleet.FieldAccountID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value.Valid {
				_m.AccountID = value.String
			}
		case fleet.FieldDeleteTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field delete_time", values[i])
			} else if value.Valid {
				_m.DeleteTime = new(time.Time)
				*_m.DeleteTime = value.Time
			}
		case fleet.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				_m.DisplayName = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Fleet.
// This includes values selected through modifiers, order, etc.
func (_m *Fleet) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryVehicles queries the "vehicles" edge of the Fleet entity.
func (_m *Fleet) QueryVehicles() *VehicleQuery {
	return NewFleetClient(_m.config).QueryVehicles(_m)
}

// Update returns a builder for updating this Fleet.
// Note that you need to call Fleet.Unwrap() before calling this method if this Fleet
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Fleet) Update() *FleetUpdateOne {
	return NewFleetClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Fleet entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Fleet) Unwrap() *Fleet {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Fleet is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Fleet) String() string {
	var builder strings.Builder
	builder.WriteString("Fleet(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("account_id=")
	builder.WriteString(_m.AccountID)
	builder.WriteString(", ")
	if v := _m.DeleteTime; v != nil {
		builder.WriteString("delete_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(_m.DisplayName)
	builder.WriteByte(')')
	return builder.String()
}

// Fleets is a parsable slice of Fleet.
type Fleets []*Fleet
