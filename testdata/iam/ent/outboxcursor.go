// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/infobloxopen/devedge-sdk/testdata/iam/ent/outboxcursor"
)

// OutboxCursor is the model entity for the OutboxCursor schema.
type OutboxCursor struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CursorTime holds the value of the "cursor_time" field.
	CursorTime time.Time `json:"cursor_time,omitempty"`
	// CursorID holds the value of the "cursor_id" field.
	CursorID string `json:"cursor_id,omitempty"`
	// HeadFailures holds the value of the "head_failures" field.
	HeadFailures int `json:"head_failures,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OutboxCursor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case outboxcursor.FieldHeadFailures:
			values[i] = new(sql.NullInt64)
		case outboxcursor.FieldID, outboxcursor.FieldCursorID:
			values[i] = new(sql.NullString)
		case outboxcursor.FieldCursorTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OutboxCursor fields.
func (_m *OutboxCursor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case outboxcursor.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case outboxcursor.FieldCursorTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field cursor_time", values[i])
			} else if value.Valid {
				_m.CursorTime = value.Time
			}
		case outboxcursor.FieldCursorID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cursor_id", values[i])
			} else if value.Valid {
				_m.CursorID = value.String
			}
		case outboxcursor.FieldHeadFailures:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field head_failures", values[i])
			} else if value.Valid {
				_m.HeadFailures = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OutboxCursor.
// This includes values selected through modifiers, order, etc.
func (_m *OutboxCursor) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this OutboxCursor.
// Note that you need to call OutboxCursor.Unwrap() before calling this method if this OutboxCursor
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *OutboxCursor) Update() *OutboxCursorUpdateOne {
	return NewOutboxCursorClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the OutboxCursor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *OutboxCursor) Unwrap() *OutboxCursor {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: OutboxCursor is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *OutboxCursor) String() string {
	var builder strings.Builder
	builder.WriteString("OutboxCursor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("cursor_time=")
	builder.WriteString(_m.CursorTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("cursor_id=")
	builder.WriteString(_m.CursorID)
	builder.WriteString(", ")
	builder.WriteString("head_failures=")
	builder.WriteString(fmt.Sprintf("%v", _m.HeadFailures))
	builder.WriteByte(')')
	return builder.String()
}

// OutboxCursors is a parsable slice of OutboxCursor.
type OutboxCursors []*OutboxCursor
