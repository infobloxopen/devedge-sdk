# Binaries / build output
/bin/
*.exe
*.out
*.test
# Compiled protoc plugins / tools — build from ./cmd via `make generate` (never commit)
/protoc-gen-storage
/protoc-gen-ent
/protoc-gen-svc
/protoc-gen-devedge-authz
/security-check

# Go workspace files
go.work
go.work.sum

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
docs/public/
docs/.hugo_build.lock
